services:
  lingarr-shim:
    build: .
    container_name: lingarr-shim
    network_mode: host
    environment:
      - BACK_TRANSLATOR_URL=http://192.168.1.30:8104
      - BATCH_SIZE=64
      - REQUEST_TIMEOUT=120
    restart: unless-stopped
